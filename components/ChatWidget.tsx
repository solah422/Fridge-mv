import React from 'react';

// This component has been disabled as the chat feature is not fully implemented and was causing build errors.
export const ChatWidget: React.FC = () => {
    return null;
};
